---
# A replicaset rs-d33393 is created. However the pods are not coming up. Identify and fix the issue.
# Once fixed, ensure the ReplicaSet has 4 Ready replicas.

# kubectl set image replicaset/rs-d33393 busybox-container=busybox
# kubectl scale rs rs-d33393 --replicas=0
# kubectl scale rs rs-d33393 --replicas=4

# Alternatively: kubectl delete pod --label=name=busybox-pod
